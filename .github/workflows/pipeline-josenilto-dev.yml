commit acf38fdf53075432d79bfcf7243e34bf5cd582e2
Author: Josenilto Luis <josenilto@outlook.com>
Date:   Sun May 31 22:09:33 2026 -0300

    fix: pipelines hmg/dev buscam portfolio do prd via git show
    
    - HMG: fetch-depth:0 + git show origin/prd para todos os artefatos
    - DEV: git fetch origin prd + git show para index.html/404/health/assets
    - Resolve 404 em josenilto.github.io/dev e /hmg
    
    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

diff --git a/.github/workflows/pipeline-josenilto-dev.yml b/.github/workflows/pipeline-josenilto-dev.yml
index 874bef8..c2079c4 100644
--- a/.github/workflows/pipeline-josenilto-dev.yml
+++ b/.github/workflows/pipeline-josenilto-dev.yml
@@ -275,23 +275,27 @@ jobs:
           name: dist-josenilto-dev
           path: dev/dist/
 
+      - name: 🔄 Buscar branch prd para arquivos do portfólio
+        run: git fetch origin prd
+
       # ── Monta estrutura de publicação ───────────────────────
       # _site-jdev/            → raiz de josenilto-dev.github.io
-      # _site-jdev/index.html  → portfólio estático (site principal)
-      # _site-jdev/assets/     → CSS, JS, imagens do portfólio
+      # _site-jdev/index.html  → portfólio estático (fonte: prd)
+      # _site-jdev/assets/     → CSS, JS, imagens (fonte: prd)
       # _site-jdev/app/        → React app compilada (modo dev)
       - name: 📁 Preparar pasta de publicação
         run: |
           mkdir -p _site-jdev/app
 
-          # ── Site estático (portfólio principal) ──
-          cp index.html       _site-jdev/
-          cp 404.html         _site-jdev/
-          cp health           _site-jdev/
-          cp -r assets/       _site-jdev/assets/
+          # ── Portfólio: sempre do branch prd ──
+          for f in index.html 404.html health robots.txt; do
+            git show origin/prd:"$f" > "_site-jdev/$f"
+          done
+          git checkout origin/prd -- assets
+          cp -r assets/ _site-jdev/assets/
 
           # ── React app compilada (subpasta /app/) ──
-          cp -r dev/dist/.    _site-jdev/app/
+          cp -r dev/dist/. _site-jdev/app/
 
           echo "📂 Estrutura de publicação DEV:"
           find _site-jdev -maxdepth 3 -type f | head -30
